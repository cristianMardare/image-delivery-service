{
  "name": "image-delivery-service",
  "version": "0.0.1",
  "description": "An image delivery service for Ownzones Node.js Challenge",
  "main": "dist/server.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "npm run build-ts && node dist/server.js",
    "build-ts": "tsc",
    "watch-ts": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:reload": "nodemon --watch src -e ts --exec npm run start"
  },
  "author": "Cristian Mardare",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "4.17.1",
    "typescript": "3.8.3"
  },
  "dependencies": {
    "express": "4.17.1",
    "nodemon": "2.0.2"
  }
}
